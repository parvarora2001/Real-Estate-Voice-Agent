---
description: Update CLAUDE.md Session Log with this session's changes before exiting
---

Wrap up the current session by updating `CLAUDE.md` in the project root.

Do the following:

1. Open `CLAUDE.md` and find the **Session Log** section at the bottom.
2. Add a new entry headed with the real current date (`### YYYY-MM-DD`). If an entry
   for today already exists, append to it instead of duplicating the heading.
3. In the entry, concisely summarize this session:
   - Files added / modified / deleted and why.
   - Decisions made and any context a future session would need.
   - Anything left unfinished or any follow-ups.
4. If anything earlier in `CLAUDE.md` (architecture, key files, commands, conventions,
   limitations) became stale during this session, update it in place.
5. Save the file.
6. Briefly confirm to me what you logged.

Keep it concise — bullets, intent over raw diffs. Do not just restate git history.

$ARGUMENTS
